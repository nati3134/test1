pipeline{
    agent {
        docker 'docker build -t liran .'
    }
    stages {
        stage("clean") {
            steps {
                deleteDir()
            }
        }
        stage('clone repo'){
            steps{
                bat "git clone https://github.com/liran774/python_test.git"
            }
        }
        
        }
    }
